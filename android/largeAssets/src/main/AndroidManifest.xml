<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.deferred_crash.largeAssets">

    <application
        android:hasCode="false">
    </application>

    <dist:module
        dist:instant="false"
        dist:title="@string/largeAssetsName">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>
</manifest>
